#include <iostream>

long longest_palin(string& s){
    long sy_range = 0;
    long res = 0;
    long expect = s.size();

    long pivot = s.size()/2;
    while (expect < res) {
        if (sy_range) {
            
        }

    }

}